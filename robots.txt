# glow — Beauty, Nails & More
# Allow every crawler to index the whole site.
#
# Note: Cloudflare prepends its own "managed" block above this one,
# which disallows AI-training crawlers (GPTBot, CCBot, ClaudeBot,
# Google-Extended, …). That does not affect Google Search indexing —
# Googlebot is covered by the "User-agent: *  Allow: /" rule.

User-agent: *
Allow: /

Sitemap: https://www.glowbeautynails.gr/sitemap.xml
